spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  data:
    redis:
      host: redis
      port: 6379

  config:
    import:
      - config/omok/db.yml

logging:
  file:
    name: /app/logs/application.log
